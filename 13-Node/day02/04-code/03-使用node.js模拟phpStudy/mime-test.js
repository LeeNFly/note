var mime = require('mime');

// mime.getType    根据文件的路径 或者文件的后缀名 获取文件对应的Content-Type
// mime.getExtension

// console.log(mime.getType("abc/index.swf"))
// console.log(mime.getExtension("text/css"));
